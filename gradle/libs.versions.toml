[versions]
agp = "8.8.0"
kotlin = "2.1.0"
ksp = "2.1.0-1.0.29"
golang = "1.0.4"
coroutine = "1.10.2"
coreKtx = "1.16.0"
activity = "1.10.1"
fragment = "1.8.9"
appcompat = "1.7.1"
coordinator = "1.3.0"
# Material Components (XML-слой на Theme.MaterialComponents.*).
#
# 1.13.0 собран с non-transitive R и больше не переэкспортирует атрибуты
# appcompat — colorPrimary и colorControlNormal через него не прочитать.
# Единственным таким читателем был design/adapter/PopupListAdapter.kt,
# удалённый вместе с переездом экранов на Compose; в Kotlin остались только
# обращения к android.R.attr, а темы в разметке берут атрибуты у appcompat
# напрямую (он прописан и в design, и в app).
#
# По метаданным артефакта потолка нет: minCompileSdk=1, minAGP=1.0.0.
# Из зависимостей приходят constraintlayout 2.1.0 и graphics-shapes 1.0.1 —
# на compileSdk 35 и AGP 8.8.0 не влияют.
material = "1.13.0"
# Потолок для компилятора Kotlin 2.1.0: 1.8.0 тянет ровно kotlin-stdlib 2.1.0,
# 1.8.1 подтянула бы 2.1.20, а с 1.9.0 метаданные уже 2.2 и их не прочитать.
serialization = "1.8.0"
kaidl = "1.15"
# JUnit 4, а не 5: Android Gradle Plugin запускает unit-тесты через JUnit 4
# из коробки, а JUnit 5 требует стороннего плагина и своего runner'а. Проверяем
# чистые считалки, и ничего из JUnit 5 им не нужно.
junit = "4.13.2"
# 2.7.2. Проверено по метаданным артефактов, а не по документации:
# room-runtime-android требует compileSdk 34 (у нас 35), AGP 1.0.0, и тянет
# kotlin-stdlib 2.1.10 — та же ветка 2.1, что и компилятор, метаданные читаются.
# room-compiler собран под KSP-API 2.0.10-1.0.24, у нас KSP новее.
#
# 2.8.x по-прежнему нельзя: room-compiler тянет kotlin-stdlib 2.2.0.
#
# ВАЖНО: с 2.7 у Room другой генератор и другой слой доступа к SQLite
# (androidx.sqlite 2.5.x). Схему мы не экспортируем, версия базы прежняя,
# миграция 1→2 на месте, но обновление ПОВЕРХ УСТАНОВЛЕННОЙ сборки нужно
# проверить руками: CI видит только компиляцию.
room = "2.7.2"
multiprocess = "1.0.0"
quickie = "1.11.0"
androidx-activity-ktx = "1.10.1"
# Jetpack Compose. BOM выравнивает версии всех compose-артефактов между собой.
# Канал СТАБИЛЬНЫЙ (compose-bom, не compose-bom-alpha): 2026.06.01 = material3 1.4.0
# + compose ui/foundation/runtime 1.11.4.
#
# Про Material 3 Expressive: в material3 1.4.0 его НЕТ. Проверено по содержимому
# material3-android-1.4.0.aar — ни LoadingIndicator, ни wavy-индикаторов, ни
# ButtonGroup/ToggleButton/MaterialShapes/FloatingToolbar/SplitButton; лежат только
# токены-заготовки, а MaterialExpressiveTheme объявлен internal. Всё это живёт
# в 1.5.0-alpha, причём с alpha19 артефакт требует compileSdk 37 и AGP 9.1.0
# (у нас 35 и 8.8.0, и плагин golang 1.0.4 такой подъём не переживёт).
# Последняя пригодная для нас альфа — 1.5.0-alpha18 (compileSdk 35, AGP 8.6.0).
#
# Потолки версий ниже держат compileSdk 35 И AGP 8.8.0 сразу: core-ktx 1.17+ и
# activity-compose 1.11+ требуют compileSdk 36 и AGP 8.9.1, material3 1.5.0-alpha19+ —
# compileSdk 37 и AGP 9.1.0. Поднять один compileSdk будет мало.
composeBom = "2026.06.01"
activityCompose = "1.10.1"
lifecycleCompose = "2.9.4"
navigationCompose = "2.9.8"
sentry = "8.53.0"

[libraries]
build-android = { module = "com.android.tools.build:gradle", version.ref = "agp" }
build-kotlin-common = { module = "org.jetbrains.kotlin:kotlin-gradle-plugin", version.ref = "kotlin" }
build-kotlin-serialization = { module = "org.jetbrains.kotlin:kotlin-serialization", version.ref = "kotlin" }
build-kotlin-compose = { module = "org.jetbrains.kotlin:compose-compiler-gradle-plugin", version.ref = "kotlin" }
build-ksp = { module = "com.google.devtools.ksp:symbol-processing-gradle-plugin", version.ref = "ksp" }
build-golang = { module = "com.github.kr328.golang:gradle-plugin", version.ref = "golang" }
kotlin-coroutine = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-android", version.ref = "coroutine" }
kotlin-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "serialization" }
androidx-core = { module = "androidx.core:core-ktx", version.ref = "coreKtx" }
androidx-activity = { module = "androidx.activity:activity", version.ref = "activity" }
androidx-fragment = { module = "androidx.fragment:fragment", version.ref = "fragment" }
androidx-appcompat = { module = "androidx.appcompat:appcompat", version.ref = "appcompat" }
androidx-coordinator = { module = "androidx.coordinatorlayout:coordinatorlayout", version.ref = "coordinator" }
androidx-room-compiler = { module = "androidx.room:room-compiler", version.ref = "room" }
androidx-room-runtime = { module = "androidx.room:room-runtime", version.ref = "room" }
androidx-room-ktx = { module = "androidx.room:room-ktx", version.ref = "room" }
google-material = { module = "com.google.android.material:material", version.ref = "material" }
junit = { module = "junit:junit", version.ref = "junit" }
kaidl-compiler = { module = "com.github.kr328.kaidl:kaidl", version.ref = "kaidl" }
kaidl-runtime = { module = "com.github.kr328.kaidl:kaidl-runtime", version.ref = "kaidl" }
rikkax-multiprocess = { module = "dev.rikka.rikkax.preference:multiprocess", version.ref = "multiprocess" }
quickie-bundled = { group = "io.github.g00fy2.quickie", name = "quickie-bundled", version.ref = "quickie" }
androidx-activity-ktx = { group = "androidx.activity", name = "activity-ktx", version.ref = "androidx-activity-ktx" }

# Jetpack Compose. Версии без version.ref берёт BOM.
androidx-compose-bom = { module = "androidx.compose:compose-bom", version.ref = "composeBom" }
androidx-compose-runtime = { module = "androidx.compose.runtime:runtime" }
androidx-compose-ui = { module = "androidx.compose.ui:ui" }
androidx-compose-ui-graphics = { module = "androidx.compose.ui:ui-graphics" }
androidx-compose-ui-tooling = { module = "androidx.compose.ui:ui-tooling" }
androidx-compose-ui-tooling-preview = { module = "androidx.compose.ui:ui-tooling-preview" }
androidx-compose-foundation = { module = "androidx.compose.foundation:foundation" }
androidx-compose-material3 = { module = "androidx.compose.material3:material3" }
androidx-activity-compose = { module = "androidx.activity:activity-compose", version.ref = "activityCompose" }
androidx-lifecycle-viewmodel-compose = { module = "androidx.lifecycle:lifecycle-viewmodel-compose", version.ref = "lifecycleCompose" }
androidx-lifecycle-runtime-compose = { module = "androidx.lifecycle:lifecycle-runtime-compose", version.ref = "lifecycleCompose" }
androidx-navigation-compose = { module = "androidx.navigation:navigation-compose", version.ref = "navigationCompose" }
sentry-android = { module = "io.sentry:sentry-android", version.ref = "sentry" }

[plugins]
